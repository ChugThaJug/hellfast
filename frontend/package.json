{
	"name": "hellfast-static",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "mkdir -p dist && cp index.html dist/"
	},
	"devDependencies": {
		"vite": "^5.0.0"
	}
}
