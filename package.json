{
  "name": "hellfast",
  "version": "1.0.0",
  "description": "Hellfast application",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
